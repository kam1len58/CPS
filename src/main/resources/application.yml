spring:
    profiles:
        active: dev
    application:
        name: "Demo"
        version: "0.1"
---
spring:
    config:
        activate:
            on-profile: dev
    servlet:
        multipart:
            location: ${user.dir}/uploads
            max-file-size: 10MB
            max-request-size: 10MB
---
server:
    port: 8080
---
spring:
    config:
        activate:
            on-profile: production
server:
    port: 9001
---
spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/student
        username: postgres
        password: 20042005kmA
        driver-class-name: org.postgresql.Driver
    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: update
---
jwt:
    secret: be1128fd75eab9b69611378c4879a12bf6beda080fa748de2b9d6aeeb62d04cb
    access:
        duration:
            minute: 5
            second: 300
        cookie-name: access-token
    refresh:
        duration:
            days: 7
            second: 604800
        cookie-name: refresh-token

---
report:
  storage:
    path: ./reports/
    retention-days: 30
  default:
    font-family: Arial
    font-size: 12
  watermark:
    enabled: false
    text: "SYSTEM PROJECT TIME TRACKING"
---
bot:
    username: CPS_TimeTrackerBot
    token: 8591981372:AAFIqUN7tLgj_nw5npxCKLnukR2WeeHiqUE
    adminChatId: 618114586

---
logging:
  file:
    name: logs/time-tracking-system.log
  level:
    com.example.demo: INFO
    com.example.demo.controller: DEBUG
    com.example.demo.service: DEBUG
    org.springframework.web: WARN
    org.hibernate: WARN
  
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 30